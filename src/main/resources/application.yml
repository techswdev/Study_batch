spring:
  profiles:
    active: local
  batch:
    job:
      names: ${job.name:NONE}
      # job.name Of Program Agurment != null ? job.name : NONE
      # if job.name => NONE (No batch operation)
---
spring:
  profiles: local
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://localhost:3306/spring_batch
      username: root
      password: 1234567890
      driver-class-name: com.mysql.jdbc.Driver